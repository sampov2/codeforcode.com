# codeforcode.com

## Install deps

```shell
sudo gem install bundler Jekyll
sudo bundle install
```

## Serve in dev mode

```shell
bundle exec jekyll serve
```

## Build site

```shell
bundle exec jekyll build
```


